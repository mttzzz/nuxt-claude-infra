# nuxt-claude-infra v0.4 — Package Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать v0.4.0 пакета `@mttzzz/nuxt-claude-infra` — вынести test-helpers, конфиги vitest/playwright и docker-compose template в пакет, чтобы три проекта (ai/easy2/kp) могли затем мигрировать на минимум проектных файлов.

**Architecture:** Public API расширяется новыми exports (`lib/test-stack`, `lib/db`, `lib/docker`, `lib/e2e`, `configs/*`, `templates/*`). Существующие хуки получают early-return для не-инфра cwd через новый `lib/is-infra-project`. TDD: для каждой чистой функции — failing test → implement → green → commit. Конфиг-пресеты и template — implementation + smoke.

**Tech Stack:** TypeScript (ESNext, strict, Bundler resolution), Bun (test runner + bin), Zod 4 (schema), `tsc` build → `dist/`, semver-теги для распространения через GitHub. Совместимость с Node-runtime (хуки запускаются `node`, не `bun`).

**Spec:** `docs/superpowers/specs/2026-05-05-nuxt-claude-infra-v0.4-design.md` — точные API-сигнатуры и обоснование архитектурных решений.

**Out of scope (отдельные планы):** миграция ai/easy2/kp проектов, глобальная установка хуков в `~/.claude/settings.json`, финальный SKILL.md update.

---

## File Structure

```
src/
├── index.ts                              # MODIFY: re-export новых API
├── config.ts                             # без изменений
├── lib/
│   ├── is-infra-project.ts               # NEW: detect infra-project by cwd marker
│   ├── db.ts                             # NEW: generic DB helpers
│   ├── docker.ts                         # NEW: parameterized docker compose ops
│   ├── e2e.ts                            # NEW: useSharedNuxt wrapper
│   ├── test-stack.ts                     # NEW: defineTestStack/start/stop with retry
│   └── (existing files unchanged)
├── configs/                              # NEW DIRECTORY
│   ├── vitest.ts                         # NEW: defineVitestPreset
│   ├── playwright.ts                     # NEW: definePlaywrightPreset
│   ├── vitest-global-setup.ts            # NEW: vitest globalSetup using test-stack
│   ├── playwright-global-setup.ts        # NEW: playwright globalSetup using test-stack
│   └── playwright-global-teardown.ts     # NEW: noop
├── templates/                            # NEW DIRECTORY (non-TS, copied to dist as-is)
│   └── docker-compose.test.yml           # NEW: base compose template
├── hooks/
│   ├── session-start.ts                  # MODIFY: early-return on non-infra
│   ├── session-end.ts                    # MODIFY: early-return on non-infra
│   ├── post-tool-use.ts                  # MODIFY: early-return on non-infra
│   └── pre-tool-use.ts                   # без изменений (уже cwd-agnostic)
└── cli/                                  # без изменений

test/
├── unit/
│   ├── is-infra-project.test.ts          # NEW
│   ├── db.test.ts                        # NEW
│   ├── docker.test.ts                    # NEW
│   ├── test-stack.test.ts                # NEW
│   └── (existing files unchanged)
└── integration/                          # NEW DIRECTORY
    └── configs.test.ts                   # NEW: smoke vitest/playwright presets

package.json                              # MODIFY: exports subpaths, files, version → 0.4.0
tsconfig.build.json                       # MODIFY: include templates/ as static asset (or use postbuild copy)
README.md                                 # MODIFY: document v0.4 API
```

**Deliverable:** тэг `v0.4.0` в репо `nuxt-claude-infra` с green test:* и build → `dist/`.

---

## Task 1: Создание feature-ветки и baseline

**Files:** (только git-операции)

- [ ] **Step 1: Убедиться, что main чистый и up-to-date**

Run:
```bash
cd ~/projects/nuxt-claude-infra
git status
git fetch origin && git pull --rebase origin main
```

Expected: `nothing to commit, working tree clean`, после `pull --rebase`: `Already up to date` или fast-forward без конфликтов.

- [ ] **Step 2: Создать feature-ветку**

Run:
```bash
git checkout -b feat/v0.4-globalize
```

Expected: `Switched to a new branch 'feat/v0.4-globalize'`

- [ ] **Step 3: Прогнать baseline-тесты**

Run:
```bash
bun test
bun run typecheck
```

Expected: все существующие тесты (`config.test.ts`, `harness-pid.test.ts`) PASS, typecheck без ошибок. Это baseline — после каждой задачи мы хотим оставаться на этом уровне.

- [ ] **Step 4: Установить дополнительные peer-deps для конфиг-пресетов (через devDependencies)**

Конфиг-пресеты будут импортировать типы из vitest и @playwright/test, но эти пакеты не должны быть hard-deps. Делаем их optionalPeerDependencies + devDependencies:

```bash
bun add -d vitest @playwright/test @nuxt/test-utils
```

Expected: добавлены в `devDependencies`. В коде конфигов будем использовать `import type` чтобы не тащить runtime-импорт.

- [ ] **Step 5: Commit baseline**

```bash
git add package.json bun.lock
git commit -m "chore: prep v0.4 — add vitest/playwright/nuxt-test-utils as devDeps"
```

Expected: коммит создан.

---

## Task 2: `lib/is-infra-project.ts` — детектор инфра-проекта

**Files:**
- Create: `src/lib/is-infra-project.ts`
- Test: `test/unit/is-infra-project.test.ts`

Функция возвращает `true`, если в указанной директории есть `.claude-infra.json` ИЛИ зависимость `@mttzzz/nuxt-claude-infra` в `package.json` (как dependency, devDependency или peerDependency). Используется хуками для early-return на не-инфра проектах.

- [ ] **Step 1: Написать failing test**

Create `test/unit/is-infra-project.test.ts`:

```ts
import { describe, expect, it } from 'bun:test'
import { mkdtempSync, rmSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'

import { isInfraProject } from '../../src/lib/is-infra-project'

function makeDir(): string {
  return mkdtempSync(join(tmpdir(), 'nci-infra-'))
}

describe('isInfraProject', () => {
  it('false когда cwd пуст', () => {
    const dir = makeDir()
    try {
      expect(isInfraProject(dir)).toBe(false)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })

  it('true когда есть .claude-infra.json (даже пустой)', () => {
    const dir = makeDir()
    try {
      writeFileSync(join(dir, '.claude-infra.json'), '{}', 'utf8')
      expect(isInfraProject(dir)).toBe(true)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })

  it('true когда package.json имеет @mttzzz/nuxt-claude-infra в dependencies', () => {
    const dir = makeDir()
    try {
      writeFileSync(
        join(dir, 'package.json'),
        JSON.stringify({ dependencies: { '@mttzzz/nuxt-claude-infra': 'github:...' } }),
        'utf8',
      )
      expect(isInfraProject(dir)).toBe(true)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })

  it('true когда package.json имеет пакет в devDependencies', () => {
    const dir = makeDir()
    try {
      writeFileSync(
        join(dir, 'package.json'),
        JSON.stringify({ devDependencies: { '@mttzzz/nuxt-claude-infra': '*' } }),
        'utf8',
      )
      expect(isInfraProject(dir)).toBe(true)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })

  it('false когда package.json есть, но без нашего пакета', () => {
    const dir = makeDir()
    try {
      writeFileSync(
        join(dir, 'package.json'),
        JSON.stringify({ dependencies: { vue: '^3.0' } }),
        'utf8',
      )
      expect(isInfraProject(dir)).toBe(false)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })

  it('false при невалидном JSON в package.json', () => {
    const dir = makeDir()
    try {
      writeFileSync(join(dir, 'package.json'), '{invalid json', 'utf8')
      expect(isInfraProject(dir)).toBe(false)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test test/unit/is-infra-project.test.ts`
Expected: FAIL with `Cannot find module '../../src/lib/is-infra-project'`.

- [ ] **Step 3: Implement `src/lib/is-infra-project.ts`**

```ts
import { existsSync, readFileSync } from 'node:fs'
import { join } from 'node:path'

const PACKAGE_NAME = '@mttzzz/nuxt-claude-infra'

/*
 * Возвращает true, если cwd-директория относится к проекту с per-session test-инфрой:
 *   1. есть `.claude-infra.json` (явный маркер), либо
 *   2. в `package.json` есть зависимость на этот пакет (любая секция).
 *
 * Используется хуками для early-return на не-инфра проектах:
 * глобально зарегистрированные хуки в `~/.claude/settings.json` срабатывают
 * во ВСЕХ Claude-сессиях, но реальная логика выполняется только когда cwd —
 * инфра-проект.
 */
export function isInfraProject(cwd: string = process.cwd()): boolean {
  if (existsSync(join(cwd, '.claude-infra.json'))) return true

  const pkgPath = join(cwd, 'package.json')
  if (!existsSync(pkgPath)) return false

  try {
    const pkg = JSON.parse(readFileSync(pkgPath, 'utf8')) as {
      dependencies?: Record<string, string>
      devDependencies?: Record<string, string>
      peerDependencies?: Record<string, string>
    }
    return Boolean(
      pkg.dependencies?.[PACKAGE_NAME] ??
      pkg.devDependencies?.[PACKAGE_NAME] ??
      pkg.peerDependencies?.[PACKAGE_NAME],
    )
  } catch {
    return false
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test test/unit/is-infra-project.test.ts`
Expected: 6/6 PASS.

- [ ] **Step 5: Run full test + typecheck**

Run: `bun test && bun run typecheck`
Expected: всё green, regression нет.

- [ ] **Step 6: Commit**

```bash
git add src/lib/is-infra-project.ts test/unit/is-infra-project.test.ts
git commit -m "feat: add lib/is-infra-project for hook early-return on non-infra cwd"
```

---

## Task 3: Hooks — early-return на не-инфра проектах

**Files:**
- Modify: `src/hooks/session-start.ts`
- Modify: `src/hooks/session-end.ts`
- Modify: `src/hooks/post-tool-use.ts`
- Test: `test/unit/hooks-early-return.test.ts` (новый)

`pre-tool-use.ts` НЕ модифицируется — он cwd-agnostic (matches любые SQL-MCP инструменты в любом проекте, защита dev-БД от DROP/TRUNCATE — полезна везде).

- [ ] **Step 1: Написать failing test**

Create `test/unit/hooks-early-return.test.ts`:

```ts
import { describe, expect, it } from 'bun:test'
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'

import { isInfraProject } from '../../src/lib/is-infra-project'

/*
 * Этот тест — smoke-уровень. Хуки выполняют I/O (читают stdin, пишут files),
 * поэтому полный e2e-тест runtime'а хуков делается в integration-тестах
 * (или ручным smoke). Здесь проверяем только логику early-return.
 */
describe('hooks early-return на non-infra cwd', () => {
  it('isInfraProject(empty dir) === false (smoke)', () => {
    const dir = mkdtempSync(join(tmpdir(), 'nci-hook-er-'))
    try {
      expect(isInfraProject(dir)).toBe(false)
    } finally {
      rmSync(dir, { recursive: true, force: true })
    }
  })
})
```

- [ ] **Step 2: Run test to verify it passes (sanity check)**

Run: `bun test test/unit/hooks-early-return.test.ts`
Expected: 1/1 PASS — это sanity-чек на интеграцию `isInfraProject` с хук-стеком; реальный effect добавляется в Steps 3-5 ниже, проверяется ручным smoke в Step 6.

- [ ] **Step 3: Modify `src/hooks/session-start.ts`**

Добавить проверку перед `runSessionStart`:

```ts
#!/usr/bin/env node
// SessionStart hook (I/O wrapper).
// ... existing comment ...
import { readHookInput, silentExit } from '../lib/claude-input.js'
import { isInfraProject } from '../lib/is-infra-project.js'
import { listProcs } from '../lib/proc.js'
import { runSessionStart } from './session-start-core.js'

function isPidAlive(pid: number): boolean {
  if (!pid || pid <= 0) return false
  try {
    process.kill(pid, 0)
    return true
  } catch (err) {
    const code = (err as NodeJS.ErrnoException).code
    return code === 'EPERM'
  }
}

const input = readHookInput()

// Early-return для не-инфра проектов: хук зарегистрирован глобально
// в ~/.claude/settings.json, но реальная работа нужна только в наших проектах.
if (!isInfraProject(process.cwd())) {
  silentExit()
}

runSessionStart(input.session_id, {
  env: process.env,
  procs: listProcs(),
  selfPid: process.pid,
  isPidAlive,
})

silentExit()
```

- [ ] **Step 4: Modify `src/hooks/session-end.ts`** аналогично

Прочитать текущий файл, добавить same block после `readHookInput()` и до основной логики:

```ts
const input = readHookInput()

if (!isInfraProject(process.cwd())) {
  silentExit()
}

// ... existing logic
```

Импорт: `import { isInfraProject } from '../lib/is-infra-project.js'`.

- [ ] **Step 5: Modify `src/hooks/post-tool-use.ts`** аналогично

Same pattern.

- [ ] **Step 6: Manual smoke-test**

Run в директории, где НЕТ `.claude-infra.json` и нет нашего пакета (например, `~/`):

```bash
cd ~ && echo '{"session_id":"test-123","tool_name":"Edit"}' | node ~/projects/nuxt-claude-infra/dist/hooks/post-tool-use.js
```

Expected: silent exit (нулевой stdout, exit code 0). Никаких файлов в `~/.claude/sessions/` НЕ создаётся (но `dist/` пока ещё не пересобран — пропусти этот шаг до Task 16, или запусти через `bun src/hooks/post-tool-use.ts`).

Альтернатива (через bun, без сборки):
```bash
cd ~ && echo '{"session_id":"test-123","tool_name":"Edit"}' | bun ~/projects/nuxt-claude-infra/src/hooks/post-tool-use.ts
```

Expected: silent exit без побочных эффектов.

- [ ] **Step 7: Run typecheck + tests**

Run: `bun run typecheck && bun test`
Expected: всё green.

- [ ] **Step 8: Commit**

```bash
git add src/hooks/session-start.ts src/hooks/session-end.ts src/hooks/post-tool-use.ts test/unit/hooks-early-return.test.ts
git commit -m "feat: hooks early-return on non-infra cwd via isInfraProject"
```

---

## Task 4: `lib/db.ts` — generic DB-хелперы

**Files:**
- Create: `src/lib/db.ts`
- Test: `test/unit/db.test.ts`

Три функции: `resolveTestDbPort(sessionId?)`, `truncateAllTables(client, tables)`, `disconnectClient(client)`. Schema-агностичные — schema знает только проект.

- [ ] **Step 1: Написать failing test**

Create `test/unit/db.test.ts`:

```ts
import { describe, expect, it, mock } from 'bun:test'

import { resolveTestDbPort, truncateAllTables, disconnectClient } from '../../src/lib/db'

describe('resolveTestDbPort', () => {
  it('бросает если sessionId не передан и env не выставлен', () => {
    const origPort = process.env.TEST_POSTGRES_PORT
    delete process.env.TEST_POSTGRES_PORT
    try {
      expect(() => resolveTestDbPort(undefined, { sessionId: undefined, ports: undefined })).toThrow()
    } finally {
      if (origPort) process.env.TEST_POSTGRES_PORT = origPort
    }
  })

  it('возвращает env TEST_POSTGRES_PORT если задан', () => {
    expect(resolveTestDbPort(undefined, { sessionId: undefined, ports: { db: 3318 } })).toBe(3318)
  })

  it('берёт port из переданного аллокированного порта', () => {
    expect(resolveTestDbPort('sess-123', { sessionId: 'sess-123', ports: { db: 3320 } })).toBe(3320)
  })
})

describe('truncateAllTables', () => {
  it('builds TRUNCATE statement for given tables', async () => {
    const calls: string[] = []
    const fakeClient = {
      unsafe: (sql: string) => {
        calls.push(sql)
        return Promise.resolve()
      },
    }
    await truncateAllTables(fakeClient as any, ['users', 'sessions', 'orders'])
    expect(calls).toHaveLength(1)
    expect(calls[0]).toContain('TRUNCATE')
    expect(calls[0]).toContain('users')
    expect(calls[0]).toContain('sessions')
    expect(calls[0]).toContain('orders')
    expect(calls[0]).toContain('RESTART IDENTITY')
    expect(calls[0]).toContain('CASCADE')
  })

  it('no-op для пустого списка таблиц', async () => {
    const fakeClient = mock(() => Promise.resolve())
    await truncateAllTables({ unsafe: fakeClient } as any, [])
    expect(fakeClient).not.toHaveBeenCalled()
  })

  it('quotes table names', async () => {
    const calls: string[] = []
    const fakeClient = {
      unsafe: (sql: string) => {
        calls.push(sql)
        return Promise.resolve()
      },
    }
    await truncateAllTables(fakeClient as any, ['user'])
    expect(calls[0]).toContain('"user"')
  })
})

describe('disconnectClient', () => {
  it('вызывает client.end() если есть', async () => {
    let ended = false
    await disconnectClient({ end: async () => { ended = true } } as any)
    expect(ended).toBe(true)
  })

  it('игнорирует null/undefined client', async () => {
    await expect(disconnectClient(null as any)).resolves.toBeUndefined()
    await expect(disconnectClient(undefined as any)).resolves.toBeUndefined()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test test/unit/db.test.ts`
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Implement `src/lib/db.ts`**

```ts
import type { SessionPorts } from './ports.js'

/*
 * Минимальный интерфейс postgres-js клиента, который нам нужен.
 * Drizzle/postgres-js клиент удовлетворяет этому контракту.
 */
export interface PostgresClient {
  unsafe: (sql: string, params?: unknown[]) => Promise<unknown>
  end?: () => Promise<void>
}

export interface ResolveTestDbPortOpts {
  sessionId: string | undefined
  ports: { db: number } | undefined
}

/*
 * Возвращает порт test-Postgres'а текущей сессии.
 * Источники (в порядке приоритета):
 *   1. opts.ports.db (передан явно — например из startTestStack handle)
 *   2. process.env.TEST_POSTGRES_PORT (выставлен docker-compose'ом)
 *
 * Без обоих — throw (вызывающий должен сначала поднять стек).
 */
export function resolveTestDbPort(
  sessionId?: string,
  opts?: ResolveTestDbPortOpts,
): number {
  if (opts?.ports?.db) return opts.ports.db

  const env = process.env.TEST_POSTGRES_PORT
  if (env) {
    const port = Number(env)
    if (!Number.isNaN(port)) return port
  }

  throw new Error(
    'Невозможно определить test-Postgres порт. ' +
    'Убедись, что test-stack поднят (startTestStack) или что TEST_POSTGRES_PORT выставлен.',
  )
}

/*
 * TRUNCATE всех переданных таблиц одним statement'ом.
 * `RESTART IDENTITY CASCADE` сбрасывает sequence'ы и каскадит внешние ключи.
 * Имена таблиц quote'ятся (через ") чтобы избежать проблем с reserved words ("user").
 */
export async function truncateAllTables(
  client: PostgresClient,
  tables: readonly string[],
): Promise<void> {
  if (tables.length === 0) return
  const quoted = tables.map(t => `"${t}"`).join(', ')
  await client.unsafe(`TRUNCATE ${quoted} RESTART IDENTITY CASCADE`)
}

/*
 * Безопасный disconnect — игнорирует null/undefined и отсутствие .end().
 */
export async function disconnectClient(client: PostgresClient | null | undefined): Promise<void> {
  if (!client) return
  if (typeof client.end === 'function') {
    await client.end()
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test test/unit/db.test.ts`
Expected: 7/7 PASS.

- [ ] **Step 5: Run full test + typecheck**

Run: `bun test && bun run typecheck`
Expected: green.

- [ ] **Step 6: Commit**

```bash
git add src/lib/db.ts test/unit/db.test.ts
git commit -m "feat: add lib/db generic helpers (resolveTestDbPort, truncateAllTables, disconnectClient)"
```

---

## Task 5: `lib/docker.ts` — параметризованные docker-compose операции

**Files:**
- Create: `src/lib/docker.ts`
- Test: `test/unit/docker.test.ts`

Generic версия `test/helpers/docker.ts` из ai. Все project-specific константы (`PROJECT_PREFIX`, `IMAGE_TAG`, `COMPOSE_FILE`) приходят как аргументы или читаются из `loadProjectConfig()`.

- [ ] **Step 1: Написать failing test**

Create `test/unit/docker.test.ts`:

```ts
import { describe, expect, it } from 'bun:test'

import {
  composeProjectName,
  buildComposeEnv,
  type SpawnRunner,
} from '../../src/lib/docker'

describe('composeProjectName', () => {
  it('конкатенирует prefix + sessionId', () => {
    expect(composeProjectName('ai-pushka-test', 'abc-123')).toBe('ai-pushka-test-abc-123')
  })

  it('slug'ит sessionId если содержит unsafe-символы', () => {
    expect(composeProjectName('ai-test', 'foo_bar.baz')).toBe('ai-test-foo-bar-baz')
  })
})

describe('buildComposeEnv', () => {
  it('собирает env-vars из ports + project config', () => {
    const env = buildComposeEnv({
      ports: { test: 3210, db: 3320, redis: 6410, mcp: 3110 },
      testDbName: 'ai_pushka_test',
      imageTag: 'ai-pushka-test-server:latest',
    })
    expect(env.POSTGRES_PORT).toBe('3320')
    expect(env.REDIS_PORT).toBe('6410')
    expect(env.TEST_SERVER_PORT).toBe('3210')
    expect(env.TEST_DB_NAME).toBe('ai_pushka_test')
    expect(env.IMAGE_TAG).toBe('ai-pushka-test-server:latest')
  })
})

describe('imageExists (mock spawn)', () => {
  it('true когда docker images возвращает не-пустой output', async () => {
    const runner: SpawnRunner = (cmd, args) => {
      expect(cmd).toBe('docker')
      expect(args).toContain('images')
      return { stdout: 'sha256:abc123\n', stderr: '', status: 0 }
    }
    const { imageExists } = await import('../../src/lib/docker')
    expect(imageExists('foo:latest', runner)).toBe(true)
  })

  it('false когда docker images возвращает пустой output', async () => {
    const runner: SpawnRunner = () => ({ stdout: '', stderr: '', status: 0 })
    const { imageExists } = await import('../../src/lib/docker')
    expect(imageExists('foo:latest', runner)).toBe(false)
  })

  it('false при ненулевом exit code', async () => {
    const runner: SpawnRunner = () => ({ stdout: '', stderr: 'error', status: 1 })
    const { imageExists } = await import('../../src/lib/docker')
    expect(imageExists('foo:latest', runner)).toBe(false)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test test/unit/docker.test.ts`
Expected: FAIL — модуль отсутствует.

- [ ] **Step 3: Implement `src/lib/docker.ts`**

```ts
import { spawnSync } from 'node:child_process'

import { slugSessionId, type SessionPorts } from './ports.js'

/*
 * Минимальный интерфейс runner'а — для DI в тестах.
 * Реальный runner делегирует в node:child_process spawnSync.
 */
export interface SpawnRunner {
  (cmd: string, args: readonly string[], opts?: { env?: NodeJS.ProcessEnv; cwd?: string }): {
    stdout: string
    stderr: string
    status: number | null
  }
}

const defaultRunner: SpawnRunner = (cmd, args, opts) => {
  const result = spawnSync(cmd, [...args], { encoding: 'utf8', env: opts?.env, cwd: opts?.cwd })
  return {
    stdout: result.stdout ?? '',
    stderr: result.stderr ?? '',
    status: result.status,
  }
}

/*
 * Имя docker-compose project: <prefix>-<slug-session-id>.
 * Один project = один комплект контейнеров (postgres-test, redis-test, test-server).
 */
export function composeProjectName(prefix: string, sessionId: string): string {
  return `${prefix}-${slugSessionId(sessionId)}`
}

/*
 * Env-vars для docker-compose, который потом подставляет их в template
 * через ${VAR}-синтаксис.
 */
export function buildComposeEnv(opts: {
  ports: SessionPorts
  testDbName: string
  imageTag: string
}): Record<string, string> {
  return {
    POSTGRES_PORT: String(opts.ports.db),
    REDIS_PORT: String(opts.ports.redis),
    TEST_SERVER_PORT: String(opts.ports.test),
    TEST_DB_NAME: opts.testDbName,
    IMAGE_TAG: opts.imageTag,
  }
}

/*
 * Существует ли docker-image локально.
 */
export function imageExists(tag: string, runner: SpawnRunner = defaultRunner): boolean {
  const result = runner('docker', ['images', '-q', tag])
  return result.status === 0 && result.stdout.trim().length > 0
}

export interface BuildImageOpts {
  imageTag: string
  /** Путь к Dockerfile relative to cwd. По умолчанию `Dockerfile`. */
  dockerfile?: string
  /** Build target (для multi-stage). */
  target?: string
  /** Дополнительные --build-arg KEY=VALUE. */
  buildArgs?: Record<string, string>
  /** cwd для запуска docker build. */
  cwd?: string
}

/*
 * Сборка test-server образа. Идемпотентна: если образ существует — пропуск.
 * Передай force=true чтобы пересобрать всегда.
 */
export function buildTestServerImage(
  opts: BuildImageOpts,
  force = false,
  runner: SpawnRunner = defaultRunner,
): void {
  if (!force && imageExists(opts.imageTag, runner)) return

  const args: string[] = ['build', '-t', opts.imageTag, '-f', opts.dockerfile ?? 'Dockerfile']
  if (opts.target) args.push('--target', opts.target)
  for (const [key, value] of Object.entries(opts.buildArgs ?? {})) {
    args.push('--build-arg', `${key}=${value}`)
  }
  args.push('.')

  const result = runner('docker', args, { cwd: opts.cwd })
  if (result.status !== 0) {
    throw new Error(`docker build failed: ${result.stderr || result.stdout}`)
  }
}

export interface ComposeOpts {
  composeFile: string
  projectName: string
  env: Record<string, string>
  cwd?: string
}

/*
 * docker compose -p <name> -f <file> up -d --wait.
 * --wait блокирует до health-check'а всех healthcheck-сервисов.
 */
export function startTestStackContainers(opts: ComposeOpts, runner: SpawnRunner = defaultRunner): void {
  const result = runner(
    'docker',
    ['compose', '-p', opts.projectName, '-f', opts.composeFile, 'up', '-d', '--wait'],
    { env: { ...process.env, ...opts.env }, cwd: opts.cwd },
  )
  if (result.status !== 0) {
    throw new Error(`docker compose up failed: ${result.stderr || result.stdout}`)
  }
}

/*
 * docker compose -p <name> down -v (volume cleanup).
 */
export function stopTestStackContainers(
  opts: Omit<ComposeOpts, 'env'>,
  runner: SpawnRunner = defaultRunner,
): void {
  runner(
    'docker',
    ['compose', '-p', opts.projectName, '-f', opts.composeFile, 'down', '-v', '--remove-orphans'],
    { cwd: opts.cwd },
  )
  // Игнорируем status — down может быть idempotent no-op.
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test test/unit/docker.test.ts`
Expected: 7/7 PASS.

- [ ] **Step 5: Run full test + typecheck**

Expected: green.

- [ ] **Step 6: Commit**

```bash
git add src/lib/docker.ts test/unit/docker.test.ts
git commit -m "feat: add lib/docker (composeProjectName, buildTestServerImage, start/stopTestStackContainers)"
```

---

## Task 6: `lib/test-stack.ts` — `defineTestStack` с retry

**Files:**
- Create: `src/lib/test-stack.ts`
- Test: `test/unit/test-stack.test.ts`

Главная factory — orchestrator для allocate→build→up. Включает retry при port-conflict (логика из easy2).

- [ ] **Step 1: Написать failing test**

Create `test/unit/test-stack.test.ts`:

```ts
import { describe, expect, it } from 'bun:test'

import {
  allocateWithRetry,
  type PortAllocator,
  type ContainerStarter,
  type PortFreer,
} from '../../src/lib/test-stack'
import type { SessionPorts } from '../../src/lib/ports'

describe('allocateWithRetry', () => {
  const goodPorts: SessionPorts = { mcp: 3110, test: 3210, db: 3320, redis: 6410 }

  it('успех с первой попытки', async () => {
    let allocCalls = 0, freeCalls = 0, startCalls = 0
    const allocator: PortAllocator = async () => { allocCalls++; return goodPorts }
    const starter: ContainerStarter = async () => { startCalls++ }
    const freer: PortFreer = async () => { freeCalls++ }

    const ports = await allocateWithRetry('sess-1', { allocator, starter, freer, maxAttempts: 3 })
    expect(ports).toEqual(goodPorts)
    expect(allocCalls).toBe(1)
    expect(startCalls).toBe(1)
    expect(freeCalls).toBe(0)
  })

  it('retry один раз при port-conflict, успех на второй', async () => {
    let allocCalls = 0, freeCalls = 0, startCalls = 0
    const allocator: PortAllocator = async () => { allocCalls++; return goodPorts }
    const starter: ContainerStarter = async () => {
      startCalls++
      if (startCalls === 1) throw new Error('Bind for 0.0.0.0:3320 failed: port is already allocated')
    }
    const freer: PortFreer = async () => { freeCalls++ }

    const ports = await allocateWithRetry('sess-1', { allocator, starter, freer, maxAttempts: 3 })
    expect(ports).toEqual(goodPorts)
    expect(allocCalls).toBe(2)
    expect(startCalls).toBe(2)
    expect(freeCalls).toBe(1)
  })

  it('пробрасывает ошибку, не относящуюся к port-conflict', async () => {
    const allocator: PortAllocator = async () => goodPorts
    const starter: ContainerStarter = async () => { throw new Error('image not found') }
    const freer: PortFreer = async () => {}

    await expect(allocateWithRetry('sess-1', { allocator, starter, freer, maxAttempts: 3 })).rejects.toThrow('image not found')
  })

  it('сдаётся после maxAttempts при постоянном port-conflict', async () => {
    let attempts = 0
    const allocator: PortAllocator = async () => goodPorts
    const starter: ContainerStarter = async () => {
      attempts++
      throw new Error('port is already allocated')
    }
    const freer: PortFreer = async () => {}

    await expect(allocateWithRetry('sess-1', { allocator, starter, freer, maxAttempts: 3 })).rejects.toThrow(/port/i)
    expect(attempts).toBe(3)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test test/unit/test-stack.test.ts`
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Implement `src/lib/test-stack.ts`**

```ts
import { mkdirSync, writeFileSync, existsSync, readFileSync, rmSync } from 'node:fs'
import { join } from 'node:path'

import { loadProjectConfig, type ProjectConfig } from '../config.js'
import {
  allocateSessionPorts,
  freeSessionPorts,
  resolveSessionId,
  SESSIONS_DIR,
  type SessionPorts,
} from './ports.js'
import {
  buildTestServerImage,
  composeProjectName,
  buildComposeEnv,
  startTestStackContainers,
  stopTestStackContainers,
} from './docker.js'

export interface TestStackHandle {
  sessionId: string
  host: string                    // напр. "http://127.0.0.1:3210"
  ports: SessionPorts
  composeProjectName: string
}

export interface TestStackDeps {
  /** Project-specific Drizzle teardown — вызывается в stopTestStack. */
  disconnectDb?: () => Promise<void>
  /** Override imageTag (по умолчанию <dockerProjectPrefix>-server:latest). */
  imageTag?: string
  /** Build target для Dockerfile (multi-stage). */
  buildTarget?: string
  /** Build-args для docker build. */
  buildArgs?: Record<string, string>
}

/* DI-типы для тестирования retry-логики. */
export type PortAllocator = (sessionId: string) => Promise<SessionPorts>
export type ContainerStarter = (sessionId: string, ports: SessionPorts) => Promise<void>
export type PortFreer = (sessionId: string) => Promise<void>

const PORT_CONFLICT_PATTERNS = [
  /port is already allocated/i,
  /address already in use/i,
  /bind for .* failed/i,
]

function isPortConflict(err: unknown): boolean {
  const msg = err instanceof Error ? err.message : String(err)
  return PORT_CONFLICT_PATTERNS.some(p => p.test(msg))
}

export interface AllocateWithRetryOpts {
  allocator: PortAllocator
  starter: ContainerStarter
  freer: PortFreer
  maxAttempts: number
}

/*
 * Аллоцирует порты и стартует контейнеры с retry при port-conflict.
 * При port-conflict: free outgrown ports, attempt #2 с новой аллокацией.
 * Логика взята из easy2 test-stack.ts (универсализирована).
 */
export async function allocateWithRetry(
  sessionId: string,
  opts: AllocateWithRetryOpts,
): Promise<SessionPorts> {
  let lastError: unknown
  for (let attempt = 1; attempt <= opts.maxAttempts; attempt++) {
    const ports = await opts.allocator(sessionId)
    try {
      await opts.starter(sessionId, ports)
      return ports
    } catch (err) {
      lastError = err
      if (!isPortConflict(err)) throw err
      await opts.freer(sessionId)
      // Loop continues — try fresh allocation
    }
  }
  throw lastError ?? new Error('allocateWithRetry: exhausted retries without specific error')
}

interface PersistedHandle {
  sessionId: string
  host: string
  ports: SessionPorts
  composeProjectName: string
  pid: number
}

function handlePath(sessionId: string): string {
  return join(SESSIONS_DIR, sessionId, 'test-stack.json')
}

function persistHandle(handle: TestStackHandle): void {
  const dir = join(SESSIONS_DIR, handle.sessionId)
  mkdirSync(dir, { recursive: true })
  const persisted: PersistedHandle = {
    sessionId: handle.sessionId,
    host: handle.host,
    ports: handle.ports,
    composeProjectName: handle.composeProjectName,
    pid: process.pid,
  }
  writeFileSync(handlePath(handle.sessionId), JSON.stringify(persisted, null, 2), 'utf8')
}

function loadHandle(sessionId: string): PersistedHandle | null {
  const path = handlePath(sessionId)
  if (!existsSync(path)) return null
  return JSON.parse(readFileSync(path, 'utf8')) as PersistedHandle
}

/*
 * Идемпотентно поднимает per-session test-stack.
 * Если уже поднят (handle file существует и порт ещё открыт) — re-use.
 *
 * Шаги:
 *   1. resolveSessionId
 *   2. loadProjectConfig
 *   3. buildTestServerImage (idempotent)
 *   4. allocateWithRetry для allocate + compose up
 *   5. persistHandle
 *   6. health-check на test-server (HTTP GET /api/health/ready)
 */
export async function startTestStack(deps: TestStackDeps = {}): Promise<TestStackHandle> {
  const sessionId = resolveSessionId() ?? `local-${process.pid}`
  const config = await loadProjectConfig()
  const imageTag = deps.imageTag ?? `${config.dockerProjectPrefix}-server:latest`

  // Re-use если уже поднят
  const existing = loadHandle(sessionId)
  if (existing) {
    return {
      sessionId: existing.sessionId,
      host: existing.host,
      ports: existing.ports,
      composeProjectName: existing.composeProjectName,
    }
  }

  // Build image (idempotent)
  buildTestServerImage({
    imageTag,
    target: deps.buildTarget,
    buildArgs: deps.buildArgs,
  })

  // Allocate + start with retry
  const projectName = composeProjectName(config.dockerProjectPrefix, sessionId)
  const ports = await allocateWithRetry(sessionId, {
    allocator: allocateSessionPorts,
    starter: async (_sid, p) => {
      const env = buildComposeEnv({ ports: p, testDbName: config.testDbName, imageTag })
      startTestStackContainers({
        composeFile: config.paths.dockerCompose,
        projectName,
        env,
      })
    },
    freer: freeSessionPorts,
    maxAttempts: 3,
  })

  const host = `http://127.0.0.1:${ports.test}`
  const handle: TestStackHandle = { sessionId, host, ports, composeProjectName: projectName }
  persistHandle(handle)

  // Health-check (--wait в docker compose уже подождал, но дополнительно проверим)
  await waitForReady(host, 30_000)

  return handle
}

/*
 * Останавливает test-stack и снимает persisted handle.
 * Безопасно вызывать на уже-остановленном (idempotent).
 */
export async function stopTestStack(
  handleOrSessionId: TestStackHandle | string,
  deps: TestStackDeps = {},
): Promise<void> {
  const sessionId = typeof handleOrSessionId === 'string'
    ? handleOrSessionId
    : handleOrSessionId.sessionId

  const persisted = loadHandle(sessionId)
  if (!persisted) return  // Уже снят

  if (deps.disconnectDb) {
    try { await deps.disconnectDb() } catch { /* swallow during teardown */ }
  }

  const config = await loadProjectConfig()
  stopTestStackContainers({
    composeFile: config.paths.dockerCompose,
    projectName: persisted.composeProjectName,
  })

  // Cleanup persisted handle
  try { rmSync(handlePath(sessionId), { force: true }) } catch { /* swallow */ }
  await freeSessionPorts(sessionId)
}

/*
 * Factory-обёртка: deps хранится один раз, возвращает start/stop пару.
 * Удобнее в globalSetup'ах конфигов (vitest/playwright):
 *
 *   const stack = defineTestStack({ disconnectDb })
 *   await stack.start()  // в globalSetup
 *   await stack.stop()   // в teardown
 */
export interface TestStackController {
  start: () => Promise<TestStackHandle>
  stop: () => Promise<void>
  current: () => TestStackHandle | null
}

export function defineTestStack(deps: TestStackDeps = {}): TestStackController {
  let handle: TestStackHandle | null = null
  return {
    async start() {
      handle = await startTestStack(deps)
      return handle
    },
    async stop() {
      if (handle) {
        await stopTestStack(handle, deps)
        handle = null
      }
    },
    current() {
      return handle
    },
  }
}

async function waitForReady(host: string, timeoutMs: number): Promise<void> {
  const deadline = Date.now() + timeoutMs
  let lastError: unknown
  while (Date.now() < deadline) {
    try {
      const res = await fetch(`${host}/api/health/ready`)
      if (res.ok) return
      lastError = new Error(`/api/health/ready returned ${res.status}`)
    } catch (err) {
      lastError = err
    }
    await new Promise(r => setTimeout(r, 500))
  }
  throw new Error(`test-server not ready in ${timeoutMs}ms: ${lastError}`)
}
```

- [ ] **Step 3.5: Дополнить тест для defineTestStack-factory**

Добавить в `test/unit/test-stack.test.ts`:

```ts
import { defineTestStack } from '../../src/lib/test-stack'

describe('defineTestStack (factory shape)', () => {
  it('возвращает контроллер с start/stop/current', () => {
    const stack = defineTestStack()
    expect(typeof stack.start).toBe('function')
    expect(typeof stack.stop).toBe('function')
    expect(typeof stack.current).toBe('function')
    expect(stack.current()).toBeNull()
  })
})
```

(полноценная проверка start/stop — integration, не unit.)

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test test/unit/test-stack.test.ts`
Expected: 5/5 PASS (4 на retry-логике + 1 на factory shape). Реальный `startTestStack`/`stopTestStack` без unit-теста — он integration-heavy (docker, http), будет покрыт вручную в Task 16 smoke и через миграцию ai в Plan 2.

- [ ] **Step 5: Run full test + typecheck**

Expected: green.

- [ ] **Step 6: Commit**

```bash
git add src/lib/test-stack.ts test/unit/test-stack.test.ts
git commit -m "feat: add lib/test-stack with defineTestStack and port-conflict retry"
```

---

## Task 7: `lib/e2e.ts` — useSharedNuxt

**Files:**
- Create: `src/lib/e2e.ts`

Tiny wrapper над `@nuxt/test-utils/e2e`. Не нуждается в тесте (3 строки логики).

- [ ] **Step 1: Implement `src/lib/e2e.ts`**

```ts
/*
 * Shared Nuxt setup для integration-тестов.
 * Вызывает @nuxt/test-utils/e2e setup() с host из NUXT_TEST_HOST,
 * который выставляется vitest-global-setup'ом или playwright-global-setup'ом
 * после старта test-stack.
 */
export async function useSharedNuxt(): Promise<void> {
  const host = process.env.NUXT_TEST_HOST
  if (!host) {
    throw new Error('NUXT_TEST_HOST не выставлен — globalSetup должен поднять test-stack первым')
  }
  // Lazy-import чтобы пакет не тащил @nuxt/test-utils как hard-dep.
  const { setup } = await import('@nuxt/test-utils/e2e')
  await setup({ host })
}
```

- [ ] **Step 2: Run typecheck**

Run: `bun run typecheck`
Expected: green (`@nuxt/test-utils` есть в devDeps).

- [ ] **Step 3: Commit**

```bash
git add src/lib/e2e.ts
git commit -m "feat: add lib/e2e useSharedNuxt wrapper"
```

---

## Task 8: `configs/vitest-global-setup.ts`

**Files:**
- Create: `src/configs/vitest-global-setup.ts`

GlobalSetup для vitest integration-проекта: поднять test-stack, выставить env-vars, отдать teardown.

- [ ] **Step 1: Implement**

```ts
import { defineTestStack } from '../lib/test-stack.js'

const stack = defineTestStack()

/*
 * Vitest globalSetup: возвращает teardown-функцию.
 * Vitest вызывает его один раз для всего test-run'а.
 */
export default async function globalSetup(): Promise<() => Promise<void>> {
  const handle = await stack.start()
  process.env.NUXT_TEST_HOST = handle.host
  process.env.CLAUDE_SESSION_ID = handle.sessionId
  process.env.TEST_POSTGRES_PORT = String(handle.ports.db)

  return async () => {
    await stack.stop()
  }
}
```

- [ ] **Step 2: Run typecheck**

Expected: green.

- [ ] **Step 3: Commit**

```bash
git add src/configs/vitest-global-setup.ts
git commit -m "feat: add configs/vitest-global-setup using test-stack"
```

---

## Task 9: `configs/playwright-global-setup.ts` + teardown

**Files:**
- Create: `src/configs/playwright-global-setup.ts`
- Create: `src/configs/playwright-global-teardown.ts`

- [ ] **Step 1: Implement setup**

```ts
// src/configs/playwright-global-setup.ts
import { defineTestStack } from '../lib/test-stack.js'

const stack = defineTestStack()

export default async function globalSetup(): Promise<void> {
  const handle = await stack.start()
  process.env.NUXT_TEST_HOST = handle.host
  process.env.CLAUDE_SESSION_ID = handle.sessionId
  process.env.TEST_POSTGRES_PORT = String(handle.ports.db)
}
```

- [ ] **Step 2: Implement teardown**

```ts
// src/configs/playwright-global-teardown.ts
/*
 * No-op teardown: stack живёт до SessionEnd hook'а пакета (он делает docker compose down).
 * Между прогонами тестов в одной сессии stack переиспользуется.
 */
export default async function globalTeardown(): Promise<void> {
  // intentionally empty
}
```

- [ ] **Step 3: typecheck + commit**

```bash
bun run typecheck
git add src/configs/playwright-global-setup.ts src/configs/playwright-global-teardown.ts
git commit -m "feat: add configs/playwright global setup/teardown using test-stack"
```

---

## Task 10: `configs/vitest.ts` — defineVitestPreset

**Files:**
- Create: `src/configs/vitest.ts`

- [ ] **Step 1: Implement**

```ts
import type { UserConfig } from 'vitest/config'

export interface VitestPresetOpts {
  /**
   * Project-relative paths для test-projects.
   * По умолчанию unit/component/integration в test/.
   */
  testDirs?: {
    unit?: string
    component?: string
    integration?: string
  }
  /** Дополнительный globalSetup поверх стандартного для integration. */
  extraGlobalSetup?: string[]
}

/*
 * Стандартный vitest-конфиг для Nuxt-проектов с per-session инфрой.
 *
 * Создаёт три vitest-projects:
 *   - unit: environment 'nuxt', чистая логика
 *   - component: environment 'nuxt', тесты компонентов
 *   - integration: environment 'node', с globalSetup поднимающим test-stack
 *
 * Проектная vitest.config.ts:
 *   import { defineVitestPreset } from '@mttzzz/nuxt-claude-infra/configs/vitest'
 *   export default defineVitestPreset()
 */
export function defineVitestPreset(opts: VitestPresetOpts = {}): UserConfig {
  const dirs = {
    unit: opts.testDirs?.unit ?? 'test/unit',
    component: opts.testDirs?.component ?? 'test/component',
    integration: opts.testDirs?.integration ?? 'test/integration',
  }

  const integrationGlobalSetup = [
    '@mttzzz/nuxt-claude-infra/configs/vitest-global-setup',
    ...(opts.extraGlobalSetup ?? []),
  ]

  return {
    test: {
      projects: [
        {
          test: {
            name: 'unit',
            environment: 'nuxt',
            include: [`${dirs.unit}/**/*.{test,spec}.ts`],
          },
        },
        {
          test: {
            name: 'component',
            environment: 'nuxt',
            include: [`${dirs.component}/**/*.{test,spec}.ts`],
          },
        },
        {
          test: {
            name: 'integration',
            environment: 'node',
            include: [`${dirs.integration}/**/*.{test,spec}.ts`],
            globalSetup: integrationGlobalSetup,
            poolOptions: { forks: { singleFork: true } },
          },
        },
      ],
    },
  }
}
```

- [ ] **Step 2: typecheck + commit**

```bash
bun run typecheck
git add src/configs/vitest.ts
git commit -m "feat: add configs/vitest defineVitestPreset"
```

---

## Task 11: `configs/playwright.ts` — definePlaywrightPreset

**Files:**
- Create: `src/configs/playwright.ts`

- [ ] **Step 1: Implement**

```ts
import type { PlaywrightTestConfig } from '@playwright/test'

export interface PlaywrightPresetOpts {
  /** Test directory (default: `test/e2e`). */
  testDir?: string
  /** Дополнительные projects (browsers). */
  projects?: PlaywrightTestConfig['projects']
}

/*
 * Стандартный playwright-конфиг для Nuxt-проектов с per-session инфрой.
 * GlobalSetup поднимает test-stack, NUXT_TEST_HOST читается из env.
 *
 * Проектный playwright.config.ts:
 *   import { definePlaywrightPreset } from '@mttzzz/nuxt-claude-infra/configs/playwright'
 *   export default definePlaywrightPreset()
 */
export function definePlaywrightPreset(opts: PlaywrightPresetOpts = {}): PlaywrightTestConfig {
  return {
    testDir: opts.testDir ?? './test/e2e',
    globalSetup: '@mttzzz/nuxt-claude-infra/configs/playwright-global-setup',
    globalTeardown: '@mttzzz/nuxt-claude-infra/configs/playwright-global-teardown',
    use: {
      baseURL: process.env.NUXT_TEST_HOST,
      trace: 'on-first-retry',
    },
    projects: opts.projects ?? [
      { name: 'chromium', use: { browserName: 'chromium' } },
    ],
    outputDir: '.playwright-mcp/test-results',
  }
}
```

- [ ] **Step 2: typecheck + commit**

```bash
bun run typecheck
git add src/configs/playwright.ts
git commit -m "feat: add configs/playwright definePlaywrightPreset"
```

---

## Task 12: `templates/docker-compose.test.yml`

**Files:**
- Create: `src/templates/docker-compose.test.yml`

- [ ] **Step 1: Создать template**

Create `src/templates/docker-compose.test.yml`:

```yaml
# Base docker-compose template для test-стека.
# Подключается из проектного docker-compose.test.yml через include:
#   include:
#     - path: ./node_modules/@mttzzz/nuxt-claude-infra/templates/docker-compose.test.yml
#
# Подставляемые env-vars (выставляются startTestStackContainers):
#   POSTGRES_PORT, REDIS_PORT, TEST_SERVER_PORT, TEST_DB_NAME, IMAGE_TAG
services:
  postgres-test:
    image: pgvector/pgvector:pg18
    tmpfs: ["/var/lib/postgresql/data"]
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: ${TEST_DB_NAME}
    ports:
      - "${POSTGRES_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test -d ${TEST_DB_NAME}"]
      interval: 1s
      timeout: 5s
      retries: 30

  redis-test:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT}:6379"

  test-server:
    image: ${IMAGE_TAG}
    depends_on:
      postgres-test: { condition: service_healthy }
      redis-test: { condition: service_started }
    environment:
      POSTGRES_URL: postgresql://test:test@postgres-test:5432/${TEST_DB_NAME}
      REDIS_HOST: redis-test
      REDIS_PORT: 6379
      NODE_ENV: test
      SENTRY_DISABLED: 1
      NUXT_TEST_MODE: 1
      PORT: 3000
      NO_COLOR: 1
      CI: 1
    ports:
      - "${TEST_SERVER_PORT}:3000"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:3000/api/health/ready || exit 1"]
      interval: 1s
      timeout: 5s
      retries: 60
```

- [ ] **Step 2: Validate с docker compose**

```bash
TEST_DB_NAME=test_db POSTGRES_PORT=5432 REDIS_PORT=6379 TEST_SERVER_PORT=3000 IMAGE_TAG=hello-world docker compose -f src/templates/docker-compose.test.yml config
```

Expected: распечатывает финальный YAML без ошибок (env-vars подставлены). Если есть ошибка YAML или docker-compose жалуется — поправить.

- [ ] **Step 3: Commit**

```bash
git add src/templates/docker-compose.test.yml
git commit -m "feat: add templates/docker-compose.test.yml (pgvector pg18, healthchecks, env vars)"
```

---

## Task 13: Обновить `src/index.ts` — re-export новых API

**Files:**
- Modify: `src/index.ts`

- [ ] **Step 1: Добавить новые exports**

Найти конец файла и добавить блок:

```ts
// === v0.4 additions ===

export { isInfraProject } from './lib/is-infra-project.js'

export {
  resolveTestDbPort,
  truncateAllTables,
  disconnectClient,
  type PostgresClient,
} from './lib/db.js'

export {
  composeProjectName,
  buildComposeEnv,
  imageExists,
  buildTestServerImage,
  startTestStackContainers,
  stopTestStackContainers,
  type SpawnRunner,
  type BuildImageOpts,
  type ComposeOpts,
} from './lib/docker.js'

export {
  startTestStack,
  stopTestStack,
  defineTestStack,
  allocateWithRetry,
  type TestStackHandle,
  type TestStackDeps,
  type TestStackController,
} from './lib/test-stack.js'

export { useSharedNuxt } from './lib/e2e.js'
```

Также убрать устаревший комментарий в шапке файла:

```ts
/*
 * @mttzzz/nuxt-claude-infra — public API
 *
 * v0.4: вынесены generic test-helpers (test-stack, docker, db, e2e),
 * конфиг-пресеты (vitest, playwright) и docker-compose template.
 * Хуки получили early-return на не-инфра проектах через isInfraProject.
 */
```

- [ ] **Step 2: typecheck**

Run: `bun run typecheck`
Expected: green.

- [ ] **Step 3: Commit**

```bash
git add src/index.ts
git commit -m "feat: re-export v0.4 additions from index"
```

---

## Task 14: `package.json` — exports, files, version → 0.4.0

**Files:**
- Modify: `package.json`
- Modify: `tsconfig.build.json` (для копирования templates)

- [ ] **Step 1: Обновить `package.json`**

Изменить три блока:

**`exports`** — добавить subpaths:

```json
"exports": {
  ".": {
    "types": "./dist/index.d.ts",
    "default": "./dist/index.js"
  },
  "./config": {
    "types": "./dist/config.d.ts",
    "default": "./dist/config.js"
  },
  "./hooks/*": {
    "types": "./dist/hooks/*.d.ts",
    "default": "./dist/hooks/*.js"
  },
  "./cli/*": {
    "types": "./dist/cli/*.d.ts",
    "default": "./dist/cli/*.js"
  },
  "./lib/*": {
    "types": "./dist/lib/*.d.ts",
    "default": "./dist/lib/*.js"
  },
  "./configs/*": {
    "types": "./dist/configs/*.d.ts",
    "default": "./dist/configs/*.js"
  },
  "./templates/*": "./dist/templates/*"
}
```

**`files`** — добавить templates:

```json
"files": [
  "dist/",
  "README.md",
  "LICENSE"
]
```

(`dist/` уже включает всё; templates копируются туда — см. Step 2.)

**`version`**:

```json
"version": "0.4.0"
```

- [ ] **Step 2: Обновить build-pipeline для копирования templates**

Поскольку `tsc` не копирует non-.ts файлы, добавить script:

```json
"scripts": {
  "test": "bun test",
  "typecheck": "tsc --noEmit",
  "build": "tsc -p tsconfig.build.json && bun run copy-templates",
  "copy-templates": "bun run scripts/copy-templates.ts",
  "prepack": "npm run build",
  "prepare": "npm run build"
}
```

Создать `scripts/copy-templates.ts`:

```ts
import { mkdirSync, copyFileSync, readdirSync } from 'node:fs'
import { join } from 'node:path'

const SRC = 'src/templates'
const DEST = 'dist/templates'

mkdirSync(DEST, { recursive: true })
for (const file of readdirSync(SRC)) {
  copyFileSync(join(SRC, file), join(DEST, file))
}
console.log(`Copied ${readdirSync(SRC).length} template(s) to ${DEST}/`)
```

- [ ] **Step 3: Запустить build, проверить dist/**

Run:
```bash
bun run build
ls dist/configs/ dist/templates/ dist/lib/
```

Expected: `dist/configs/vitest.js`, `dist/configs/playwright.js`, `dist/configs/vitest-global-setup.js`, `dist/configs/playwright-global-setup.js`, `dist/configs/playwright-global-teardown.js`, `dist/templates/docker-compose.test.yml`, `dist/lib/is-infra-project.js`, `dist/lib/db.js`, `dist/lib/docker.js`, `dist/lib/test-stack.js`, `dist/lib/e2e.js`. Без ошибок tsc.

- [ ] **Step 4: Commit**

```bash
git add package.json scripts/copy-templates.ts
git commit -m "chore: bump version 0.4.0, add configs/templates exports, build copies templates"
```

(`dist/` обычно gitignored, но в этом репо коммитится для git-installable workflow — будет в финальном Task 17 commit.)

---

## Task 15: README — документировать v0.4 API

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Прочитать текущий README**

Run: `cat README.md`

- [ ] **Step 2: Дописать секцию «v0.4 — Globalized API»**

В конце README, перед последней секцией ("Status" или подобной), добавить:

````markdown
## v0.4 — Globalized API

В v0.4 пакет дополняется helpers и конфиг-пресетами для нулевой копипасты в проектах.

### `defineVitestPreset()`

```ts
// project: vitest.config.ts
import { defineVitestPreset } from '@mttzzz/nuxt-claude-infra/configs/vitest'
export default defineVitestPreset()
```

Создаёт три vitest-projects (unit/component/integration) с правильным `globalSetup` для integration.

### `definePlaywrightPreset()`

```ts
// project: playwright.config.ts
import { definePlaywrightPreset } from '@mttzzz/nuxt-claude-infra/configs/playwright'
export default definePlaywrightPreset()
```

### `startTestStack()` / `stopTestStack()`

`startTestStack()` идемпотентно поднимает per-session test-стек: allocate ports → build image → docker compose up → health-check. С автоматическим retry при port-conflict (max 3 попытки).

```ts
import { startTestStack } from '@mttzzz/nuxt-claude-infra'

const handle = await startTestStack({
  disconnectDb: async () => { /* проектный Drizzle teardown */ },
})
// handle.host = "http://127.0.0.1:3210", handle.ports, handle.sessionId
```

### `templates/docker-compose.test.yml`

Базовый compose-template (pgvector/pg18 + redis + test-server) подключается из проектного compose через `include:`:

```yaml
# project: docker-compose.test.yml
include:
  - path: ./node_modules/@mttzzz/nuxt-claude-infra/templates/docker-compose.test.yml
services:
  test-server:
    environment:
      # только project-specific env-vars
      NUXT_EXCHANGE_RATE_API_URL: ${NUXT_EXCHANGE_RATE_API_URL}
```

Требует Docker Compose v2.20+ (для `include:` directive).

### Глобальные хуки в `~/.claude/settings.json`

В v0.4 хуки регистрируются один раз глобально и сами no-op'ят на не-инфра проектах через `isInfraProject(cwd)`. Установка глобально:

```bash
bun install -g github:mttzzz/nuxt-claude-infra#v0.4.0
```

Регистрация хуков — отдельный шаг (см. отдельный план миграции).
````

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: README — v0.4 globalized API section"
```

---

## Task 16: Build, full test-run, integration smoke

**Files:** (только команды)

- [ ] **Step 1: Clean build**

```bash
rm -rf dist
bun run build
```

Expected: `dist/` пересобрана, в ней все ожидаемые файлы (см. Task 14 Step 3).

- [ ] **Step 2: Full test run**

```bash
bun test
```

Expected: все unit-тесты PASS:
- existing: `config.test.ts`, `harness-pid.test.ts`
- new: `is-infra-project.test.ts`, `hooks-early-return.test.ts`, `db.test.ts`, `docker.test.ts`, `test-stack.test.ts`

- [ ] **Step 3: typecheck strict**

```bash
bun run typecheck
```

Expected: 0 ошибок.

- [ ] **Step 4: Manual hook smoke-test**

В директории, где НЕТ `.claude-infra.json`:

```bash
cd ~ && echo '{"session_id":"test","tool_name":"Edit","tool_input":{"file_path":"/tmp/x"}}' | node ~/projects/nuxt-claude-infra/dist/hooks/post-tool-use.js
```

Expected: silent exit, exit code 0, никаких файлов в `~/.claude/sessions/`.

В инфра-директории (например ai):

```bash
cd ~/projects/ai.pushka.biz && echo '{"session_id":"test-smoke","tool_name":"Edit","tool_input":{"file_path":"/tmp/x"}}' | node ~/projects/nuxt-claude-infra/dist/hooks/post-tool-use.js
```

Expected: silent exit; в `~/projects/ai.pushka.biz/.claude/sessions/test-smoke/touched.txt` появился `/tmp/x`. Удалить артефакт после теста: `rm -rf ~/projects/ai.pushka.biz/.claude/sessions/test-smoke`.

- [ ] **Step 5: Commit dist**

В этом репо `dist/` коммитится (см. v0.3.1 commit message «commit pre-built dist/ for git-installable workflow»):

```bash
git add dist/
git commit -m "build: dist for v0.4.0"
```

Если `dist/` в `.gitignore` — снять, добавить, закоммитить (один раз; затем `dist/` будет коммититься регулярно при bump'ах).

---

## Task 17: Tag v0.4.0 (manual)

**Files:** (только git-операции)

⚠️ Этот task — РУЧНОЙ. Выполняется пользователем после явного approval.

- [ ] **Step 1: Self-review всех изменений**

```bash
git log feat/v0.4-globalize ^main --oneline
git diff main...feat/v0.4-globalize --stat
```

Expected: серия коммитов от Task 1 до Task 16, понятные сообщения. Diff ratio разумный.

- [ ] **Step 2: Merge в main (или PR-flow)**

Опция A — direct merge (если main пушится напрямую):

```bash
git checkout main
git pull --rebase origin main
git merge --no-ff feat/v0.4-globalize -m "feat: v0.4.0 — globalized API (configs, templates, test-stack, hooks early-return)"
```

Опция B — через PR:

```bash
git push origin feat/v0.4-globalize
gh pr create --title "feat: v0.4.0 — globalized API" --body "$(cat <<'EOF'
## Summary
Implements v0.4.0 per spec docs/superpowers/specs/2026-05-05-nuxt-claude-infra-v0.4-design.md.

- `lib/is-infra-project` — detect infra-cwd для hook early-return
- `lib/{db,docker,test-stack,e2e}` — generic helpers
- `configs/{vitest,playwright,*-global-setup,*-global-teardown}` — конфиг-пресеты
- `templates/docker-compose.test.yml` — base compose template (pgvector/pg18)
- Hooks early-return на non-infra cwd
- README updated, version bump 0.3.1 → 0.4.0

## Test plan
- [x] All unit tests green
- [x] Typecheck strict pass
- [x] Manual hook smoke (infra and non-infra dirs)
- [ ] Plan 2 (ai migration) — отдельный спринт после merge

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 3: Тэг + push**

```bash
git tag v0.4.0
git push origin main
git push origin v0.4.0
```

Expected: GitHub видит тег `v0.4.0`, его можно использовать в других проектах через `bun add github:mttzzz/nuxt-claude-infra#v0.4.0`.

- [ ] **Step 4: Verify install**

В отдельной временной директории:

```bash
mkdir /tmp/test-nci-v0.4 && cd /tmp/test-nci-v0.4
bun init -y
bun add github:mttzzz/nuxt-claude-infra#v0.4.0
ls node_modules/@mttzzz/nuxt-claude-infra/dist/
```

Expected: пакет установлен, `dist/configs/`, `dist/templates/`, `dist/lib/is-infra-project.js` присутствуют. Cleanup: `rm -rf /tmp/test-nci-v0.4`.

---

## Definition of Done

- [ ] Все 17 tasks отмечены completed
- [ ] `git tag v0.4.0` запушен на GitHub
- [ ] Установка через `bun add github:mttzzz/nuxt-claude-infra#v0.4.0` работает
- [ ] `bun test` зелёный (все unit-тесты)
- [ ] `bun run typecheck` без ошибок
- [ ] Hook smoke-test (infra и non-infra cwd) корректен
- [ ] Готов к Plan 2 (ai migration)

После Definition of Done — переход к **Plan 2: ai.pushka.biz migration** (отдельный спек/план, который пишется на основе реального API v0.4.0 и реального опыта установки в ai через `bun link`).
